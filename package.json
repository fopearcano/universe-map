{
  "name": "universe-map",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Interactive 3D navigable HUD map of the universe — from nearby stars to the CMB — with story-voyage tracking.",
  "scripts": {
    "build:data": "node scripts/build-data.mjs",
    "build:cosmos": "node scripts/build-cosmos.mjs",
    "build:cmb": "node scripts/build-cmb.mjs",
    "build:extras": "node scripts/build-extras.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^8.1.4"
  }
}
