{
  "name": "universe-map",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Interactive 3D navigable HUD map of the known stellar neighbourhood, with story-voyage tracking.",
  "scripts": {
    "build:data": "node scripts/build-data.mjs",
    "build:cosmos": "node scripts/build-cosmos.mjs",
    "build:cmb": "node scripts/build-cmb.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
